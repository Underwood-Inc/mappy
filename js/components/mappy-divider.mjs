/**
 * `<mappy-divider>` — section rule between landing bands.
 * Copy (kicker/title) comes from light DOM; styles live in `_divider.scss`.
 *
 * CLAD tier: Atom
 */
class MappyDivider extends HTMLElement {}

customElements.define('mappy-divider', MappyDivider);
